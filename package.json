{
  "name": "sidebuyside",
  "version": "0.1.0",
  "private": true,
  "description": "A Chrome side-panel shopping comparison assistant powered by OpenRouter.",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend start",
    "test": "npm --prefix backend test"
  },
  "engines": {
    "node": ">=20.12"
  }
}
